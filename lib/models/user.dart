class User {
  String ? name;
  String ? email;
  String ? password;
  String ? passwordConfirmation;

  User({
    this.name,
    this.email,
    this.password,
    this.passwordConfirmation,
  });
}
